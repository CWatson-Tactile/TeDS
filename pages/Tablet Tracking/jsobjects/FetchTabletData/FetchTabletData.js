export default {
	TabletDataFetch: async() =>  {
		const TabletData = QueryTop5kTablets.data
		function FilterByStatus() {
			return TabletData.filter(TabletData(S) => "NEW" )}
		
		return FilterByStatus()
	}
}